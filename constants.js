const DEFAULT_FILE_NAME = "data.json";

const TIME_RANGE = {
  DAILY: "daily",
  WEEKLY: "weekly",
  MONTHLY: "monthly",
};

const DEFAUL_FILE_NAME_DELINIATOR = "-";

module.exports = {
  DEFAULT_FILE_NAME,
  TIME_RANGE,
  DEFAUL_FILE_NAME_DELINIATOR,
};
